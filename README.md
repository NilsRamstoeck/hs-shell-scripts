**WARNING: HS Pass will be stored as cleartext in .bash_hs**

To Install:

    ./install
    source ~/.bashrc

sshs requires sshpass to be installed:

    sudo apt-get install sshpass
mkc:

    mkc filename "Optional File Description"
